name: update image tag, chart version, helm package, package upload

on: 
  workflow_call:
    inputs:
      name: Update Deployment Repo Image Tag

on:
  workflow_call:
    inputs:
      image-tag:
        required: true
        type: string
      environment:
        required: true
        type: string
      deployment-repo:
        required: true
        type: string
    secrets:
      GH_PAT:
        required: true
   
